# Dashboard Architecture Diagram

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                    DESKTOP DASHBOARD (PyQt6)                    │
│                   Stock Pipeline Control Center                 │
└─────────────────────────────────────────────────────────────────┘
                              │
              ┌───────────────┴───────────────┐
              ▼                               ▼
    ┌─────────────────┐              ┌─────────────────┐
    │   Control GUI   │              │  Monitor GUI    │
    │  (User Input)   │              │ (Live Updates)  │
    └────────┬────────┘              └────────▲────────┘
             │                                 │
             │ Symbols + Settings              │ Qt Signals
             │                                 │
             ▼                                 │
    ┌─────────────────────────────────────────────────┐
    │       Pipeline Controller (QThread)             │
    │  ┌───────────────────────────────────────────┐  │
    │  │   ThreadPoolExecutor (10 Workers)         │  │
    │  │                                           │  │
    │  │  Worker 1 ─┐                             │  │
    │  │  Worker 2 ─┤  Shared Rate Limiter        │  │
    │  │  Worker 3 ─┤  (Thread-Safe)              │  │
    │  │  ...       │                             │  │
    │  │  Worker 10 ┘                             │  │
    │  └───────────────────────────────────────────┘  │
    └─────────────────────────────────────────────────┘
                     │          │
        ┌────────────┼──────────┼────────────┐
        ▼            ▼          ▼            ▼
   ┌────────┐  ┌──────────┐  ┌──────┐  ┌─────────┐
   │ EODHD  │  │ Feature  │  │ Rate │  │ MongoDB │
   │  API   │  │ Engineer │  │Limit │  │ Storage │
   └────────┘  └──────────┘  └──────┘  └─────────┘
```

## UI Component Hierarchy

```
MainWindow (QMainWindow)
│
├── QTabWidget
│   │
│   ├── Tab 1: Pipeline Control
│   │   ├── ControlPanel
│   │   │   ├── Symbol Input (QLineEdit)
│   │   │   ├── File Browser (QPushButton)
│   │   │   ├── Mode Selection (QRadioButton)
│   │   │   ├── Worker Config (QSpinBox)
│   │   │   └── Action Buttons (▶ ⏸ ⏹ 🗑)
│   │   │
│   │   └── MonitorPanel
│   │       ├── Metrics Bar (QLabel × 7)
│   │       ├── APIUsageWidget
│   │       │   ├── Daily Progress Bar
│   │       │   └── Minute Progress Bar
│   │       ├── SymbolQueueTable
│   │       │   └── QTableWidget (7 columns)
│   │       └── LogViewer
│   │           └── QTextEdit (color-coded)
│   │
│   ├── Tab 2: Database Profiles
│   │   └── ProfileBrowser
│   │       ├── Search Bar (QLineEdit)
│   │       ├── Profile Table (QTableWidget)
│   │       └── Action Buttons (View, Edit, Delete, Export)
│   │
│   └── Tab 3: Settings
│       └── SettingsPanel
│           ├── API Config (QLineEdit)
│           ├── MongoDB Config (QLineEdit)
│           ├── Pipeline Defaults (QSpinBox)
│           └── UI Settings (QRadioButton)
│
└── QStatusBar (bottom)
```

## Data Flow Diagram

```
USER INPUT
    │
    ├── Symbols: AAPL, MSFT, GOOGL
    ├── Mode: Incremental
    └── Workers: 10
    │
    ▼
┌───────────────────────────────────┐
│   PipelineController.start()      │
│   Creates ThreadPoolExecutor       │
└───────────────────────────────────┘
    │
    ├── Submit: Worker 1 → Process AAPL
    ├── Submit: Worker 2 → Process MSFT
    └── Submit: Worker 3 → Process GOOGL
    │
    ▼
┌───────────────────────────────────┐
│   Worker 1: Process AAPL          │
│                                   │
│   ┌─────────────────────────┐    │
│   │ 1. Fetch History        │    │
│   │    ↓ EODHDDataFetcher   │    │
│   │    ↓ Rate Limiter Check │    │
│   │    ↓ API Call (250×)    │    │
│   │    ↓ Returns DataFrame  │    │
│   └─────────────────────────┘    │
│                                   │
│   ┌─────────────────────────┐    │
│   │ 2. Engineer Features    │    │
│   │    ↓ FeatureEngineer    │    │
│   │    ↓ Calculate 200+ feat│    │
│   │    ↓ Returns Profile    │    │
│   └─────────────────────────┘    │
│                                   │
│   ┌─────────────────────────┐    │
│   │ 3. Store Profile        │    │
│   │    ↓ MongoDBStorage     │    │
│   │    ↓ Save to DB         │    │
│   │    ↓ Returns Success    │    │
│   └─────────────────────────┘    │
│                                   │
│   Emit: symbol_completed         │
└───────────────────────────────────┘
    │
    ▼
┌───────────────────────────────────┐
│   MonitorPanel (UI Thread)        │
│   Receives Qt Signal              │
│   Updates Table, Logs, Gauges     │
└───────────────────────────────────┘
```

## Threading Model

```
┌─────────────────────────────────────────────────────────┐
│                  MAIN THREAD (UI)                       │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    │
│  │ Control     │  │ Monitor     │  │ Profile     │    │
│  │ Panel       │  │ Panel       │  │ Browser     │    │
│  └──────┬──────┘  └──────▲──────┘  └─────────────┘    │
│         │                 │                             │
│         │ Start           │ Qt Signals                  │
│         │                 │ (Thread-Safe)               │
└─────────┼─────────────────┼─────────────────────────────┘
          │                 │
          ▼                 │
┌──────────────────────────────────────────────────┐
│         PIPELINE THREAD (QThread)                 │
│                                                   │
│  PipelineController                              │
│  │                                               │
│  ├─ Manages workers                              │
│  ├─ Emits signals ─────────────────────┘         │
│  └─ Controls ThreadPoolExecutor                  │
│      │                                            │
│      └─┬─┬─┬─┬─┬─┬─┬─┬─┬─ (10 threads)          │
│        │ │ │ │ │ │ │ │ │ │                      │
└────────┼─┼─┼─┼─┼─┼─┼─┼─┼─┼──────────────────────┘
         │ │ │ │ │ │ │ │ │ │
         ▼ ▼ ▼ ▼ ▼ ▼ ▼ ▼ ▼ ▼
┌─────────────────────────────────────────────────┐
│        WORKER POOL (10 Threads)                 │
│                                                  │
│  Worker 1  Worker 2  Worker 3  ...  Worker 10  │
│     ↓          ↓          ↓            ↓        │
│   AAPL      MSFT      GOOGL        NVDA         │
│                                                  │
│  All share:                                     │
│  ├─ AdaptiveRateLimiter (locked)                │
│  ├─ MongoDBStorage (connection pool)            │
│  └─ Pipeline instances                          │
└─────────────────────────────────────────────────┘
```

## Signal Flow

```
PipelineController              MonitorPanel
(Background Thread)             (UI Thread)
─────────────────────           ─────────────

emit(symbol_started)    ────►   Append log
                                "Starting AAPL"

emit(symbol_progress)   ────►   Update table row
                                Progress: 25%

emit(api_stats_updated) ────►   Update API gauges
                                Daily: 756/95K

emit(log_message)       ────►   Append to log viewer
                                [INFO] Fetched 390 rows

emit(symbol_completed)  ────►   Mark row green ✅
                                Update stats counter

emit(eta_updated)       ────►   Update ETA label
                                "⏱ ETA: 4m 32s"
```

## Rate Limiter Coordination

```
┌────────────────────────────────────────────────┐
│        AdaptiveRateLimiter (Shared)            │
│  ┌──────────────────────────────────────┐      │
│  │ Minute Window: [timestamps...]       │      │
│  │ Daily Calls: 756                     │      │
│  │ Lock: threading.Lock()               │      │
│  └──────────────────────────────────────┘      │
└────────────────────────────────────────────────┘
         ▲         ▲         ▲         ▲
         │         │         │         │
    ┌────┴────┬────┴────┬────┴────┬────┴────┐
    │         │         │         │         │
Worker 1  Worker 2  Worker 3  ...  Worker 10

Flow:
1. Worker 1: rate_limiter.wait_if_needed()
   → Acquires lock
   → Checks: 48 calls this minute (OK)
   → Releases lock
   → Makes API call
   → rate_limiter.record_call()

2. Worker 2: rate_limiter.wait_if_needed()
   → Acquires lock
   → Checks: 79 calls this minute (1 slot left)
   → Releases lock
   → Makes API call

3. Worker 3: rate_limiter.wait_if_needed()
   → Acquires lock
   → Checks: 80 calls (LIMIT!)
   → Sleeps 60 - elapsed + 0.5 seconds
   → Releases lock after wait
```

## Database Caching

```
┌─────────────────────────────────────────┐
│     DatabaseController                  │
│                                         │
│  ┌───────────────────────────────┐     │
│  │ Cache:                        │     │
│  │  {                            │     │
│  │    'AAPL': {...profile...}   │     │
│  │    'MSFT': {...profile...}   │     │
│  │    ...                        │     │
│  │  }                            │     │
│  │ TTL: 60 seconds               │     │
│  └───────────────────────────────┘     │
│                                         │
│  get_profile('AAPL')                   │
│    ├─ Cache hit? → Return cached       │
│    └─ Cache miss? → Query MongoDB      │
│                                         │
│  load_all_profiles()                   │
│    └─ Fetch all → Update cache         │
│                                         │
│  invalidate_cache()                    │
│    └─ Clear cache (on updates)         │
└─────────────────────────────────────────┘
              │
              ▼
      ┌─────────────┐
      │  MongoDB    │
      │             │
      │  Profiles:  │
      │  - AAPL     │
      │  - MSFT     │
      │  - GOOGL    │
      │  ...        │
      └─────────────┘
```

## File Dependencies

```
dashboard/main.py
    │
    ├── imports: PyQt6
    ├── imports: ui/main_window.py
    └── imports: utils/theme.py

ui/main_window.py
    │
    ├── imports: panels/control_panel.py
    ├── imports: panels/monitor_panel.py
    ├── imports: panels/profile_browser.py
    ├── imports: panels/settings_panel.py
    └── imports: controllers/pipeline_controller.py

controllers/pipeline_controller.py
    │
    ├── imports: config.py (existing)
    ├── imports: pipeline.py (existing)
    ├── imports: utils/rate_limiter.py (existing)
    └── imports: utils/qt_signals.py

panels/monitor_panel.py
    │
    ├── imports: widgets/symbol_queue_table.py
    ├── imports: widgets/log_viewer.py
    └── imports: widgets/api_usage_widget.py
```

## Performance Characteristics

```
Input: 100 symbols × 2 years each

┌─────────────────────────────────────────┐
│  Serial Processing (1 worker)           │
│  100 × 2 min = 200 minutes (3.3 hours)  │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  Parallel Processing (10 workers)       │
│  100 ÷ 10 × 2 min = 20 minutes          │
│  Speedup: 10× faster! ⚡                │
└─────────────────────────────────────────┘

Bottleneck: API Rate Limit (80 calls/min)
    ├─ With 10 workers, can make ~80 calls/min
    ├─ Optimal chunk: 5 days (efficient API usage)
    └─ Shared limiter prevents quota overflow
```

---

**Visual Summary**: The dashboard is a multi-threaded PyQt6 application that coordinates parallel data processing while maintaining a responsive UI through Qt's signal/slot mechanism. All workers share a single rate limiter to respect API quotas.

